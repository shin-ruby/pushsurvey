<h3>Push Name:<%=@push.name %></h3>
          <fieldset>
          	<p>
              <label for="dummy1">Category</label><br />
              <%= f.select "category_id",options_from_collection_for_select(Category.all, :id, :name) %>
            </p><hr />
            <label for="dummy1">Folder Management</label><br />
              <%= f.select "folder_id",options_from_collection_for_select(Folder.all, :id, :name) %>
            <br />
            <p>
              <label for="dummy2">Comment</label><br>
              <%= f.text_area "comment" %>
            </p>
            <p>
              <input type="reset" value="Cancel">
              <input type="submit" value="Continue">
            </p>
          </fieldset>