<fieldset>
  <legend><h4>Push Name:<%= @push.name %></h4></legend>
  <p>
    <label for="dummy1">Category</label><br/>
    <%= f.select "category_id", options_from_collection_for_select(Category.all, :id, :name, @push.category_id) %>
  </p>
  <hr/>
  <p>
    <label for="dummy1">Box Management</label><br/>
    <%= f.select "folder_id", options_from_collection_for_select(Folder.all, :id, :name, @push.folder_id) %>
  </p>
  <hr/>

  <p>
    <label for="dummy2">Comment</label><br>
    <%= f.text_area "comment" %>
  </p>

  <p>
    <input type="reset" value="Cancel">
    <input type="submit" value="Continue">
  </p>
</fieldset>
<% content_for :note_sidebar do %>
    <fieldset>
      <legend><h4>Note</h4></legend>
      <ul id="content-pushB">
        <p></p>

        <p>Category – We list out different categories for every marketing email for your convenience.
        </p>

        <p>Box Management – We create some colored boxes for you to classify every Push.
        </p>

        <p>Remarks – You can record details of every marketing email in Remarks. For example, an annual production
          release aiming at SMB customers will invite medical device users in East China…</p>
      </ul>
    </fieldset>
<% end %>