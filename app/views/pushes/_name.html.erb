<fieldset>
  <legend><h4>Create Push</h4></legend>
  <p>
    <label for="dummy0"><input type="radio" name="new_type" value="new" checked="checked">Create a new push</label><br>
    <%= f.text_field :name %>
  </p>
  <hr/>

  <p>
    <label for="dummy1"><input type="radio" name="new_type" value="copy">Copy an existing push</label><br>
    <%= select_tag(:copy_push_id, options_from_collection_for_select(Push.with_user, :id, :name)) %>
  </p>

  <p>
    <%= text_field_tag :copy_push_name %>
  </p>
  <br/>

  <p>
    <input type="reset" value="Cancel">
    <%= f.submit :value=>"Continue" %>
  </p>
</fieldset>
<% content_for :note_sidebar do %>
    <fieldset>
      <legend><h4>Note</h4></legend>
      <ul id="content">
        <p>Creating a new push is an entrance to create a successful marketing email. A complete marketing email needs
          to be related to Address Book and Design. The Address Book is for importing email address lists and the Design
          for uploading EDM designs. Once the email is sent, detailed feedbacks can be viewed. A piece of data will
          appear on the My Push page every time a Push is created and can be viewed at any time.</p>
        <%= link_to image_tag("help/push.png", :class=>"push"), root_url %>
      </ul>
    </fieldset>
<% end %>